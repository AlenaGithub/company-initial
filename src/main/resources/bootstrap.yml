---
management:
  security:
    enabled: false

security:
  basic:
    enabled: false
 
---
spring:
  application:
    name: company
  cloud:
    config:
      uri: {vcap.services.configserver-service.credentials.url}